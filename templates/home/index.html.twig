{% extends 'base.html.twig' %}

{% block title %}
	{{parent()}}Accueil
{% endblock %}

{% block navbar %}{% endblock %}
{% block content %}


	<div class="w-full h-screen object-center bg-blue-200">

		<div class="flex justify-center w-auto">
			<img src="{{ asset('/images/gsblogo.png')}}" class="flex justify-center" alt="gsblogo">
		</div>
		<div class="flex justify-center mt-5">
			<div class="max-w-sm m-4 p-10 bg-gray-500 bg-opacity-20 rounded shadow-xl">
				<div class="w-auto mx-auto flex justify-center gap-2 my-10">
					<p class="text-2xl">Bienvenue
						{{ delegue }}
						!
					</p>
				</div>

				<div class="">
					<a class="flex justify-center w-full" href="{{ path('compte_rendu') }}">
						<button type="button" class="focus:outline-none text-white text-sm py-2.5 px-5 rounded-md bg-gradient-to-r from-blue-400 to-blue-600 transform hover:scale-110">Consulter les comptes-rendus</button>
					</a>
				</div>

				<div class="mt-2">
					<a class="flex justify-center" href="{{ path('praticien') }}">
						<button type="button" class="focus:outline-none text-white text-sm py-2.5 px-5 rounded-md bg-gradient-to-r from-blue-400 to-blue-600 transform hover:scale-110">Consulter liste des praticiens</button>
					</a>
				</div>

				<div class="mt-2">
					<a class="flex justify-center" href="{{ path('logout') }}">
						<button type="button" class="focus:outline-none text-white text-sm py-2.5 px-5 rounded-md bg-gradient-to-r from-red-400 to-red-600 transform hover:scale-110">Déconnexion</button>
					</a>
				</div>

			</div>
		</div>
	</div>
</div>{% endblock %}{% block footer %}{% endblock %}
